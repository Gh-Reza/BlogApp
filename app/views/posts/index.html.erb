<section>
  <div class="user-item">
    <div class="image-box">
      <img src="<% @user.photo %>" alt="user photo">
    </div>
    <div class="user-info-box">
      <h1 class="user-name"><%= @user.name %></h1>
      <h4 class="posts-count">Number of posts: <%= @user.posts.count %></h4>
    </div>
  </div>
  <ul class="user-posts-list posts-comments">
    <% @posts.each do |post| %>
      <li class="posts-comments-box">
        <%= link_to user_post_path(@user, post) do %>
        <div class="border user-posts-item">
          <div>
            <h4>Post: #<%= post.id %></h4>
            <p><%= post.text %></p>
          </div>
          <p class="comments-likes-count">Comments: <%=post.comments.count %>, Likes: <%= post.likes.count %></p>
        </div>
        <% end %>
        <div class="border comments-box">
          <% post.comments.each do |comment| %>
            <p><%= comment.user.name %>: <%= comment.text %></p>
          <% end %>
        </div>
      </li>
    <% end %>
  </ul>
<!--  <div>-->
    <%# @posts.each do |post| %>
<!--      <div>-->
<!--        <div>-->
<!--          <h4>Post #<%#= post.id %></h4>-->
<!--          <p><%#=post.text %></p>-->
<!--          <p>Comments: <%#=post.comments.count %>, Likes: <%#= post.likes.count %></p>-->
<!--        </div>-->
<!--        <div>-->
          <%# post.comments.each do |comment| %>
<!--            <p><%#= comment.user.name %>: <%#= comment.text %></p>-->
          <%# end %>
<!--        </div>-->
<!--      </div>-->
    <%# end %>
<!--  </div>-->
</section>
